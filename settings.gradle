//include 'client'
include 'common'
include 'core-data'
include 'scheduler'
include 'server'
include 'services'
include 'tdo'

rootProject.name = 'webmetaio'

rootProject.children.each {
  it.name = rootProject.name + "-" + it.name
}

